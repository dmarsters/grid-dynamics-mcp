[project]
name = "grid-dynamics-mcp"
version = "0.1.0"
description = "MCP tool for compositional structure and mood analysis with enhanced prompt synthesis"
requires-python = ">=3.9"

dependencies = [
    "fastmcp>=0.1.0",
    "pydantic>=2.0.0",
]

[tool.uv]
managed = true
